void diodctl_serv_init (char *cfg);
int  diodctl_serv_getname (Npuser *user, u64 offset, u32 count, u8* data);
int  diodctl_serv_create (Npuser *user, char *ip);

/*
 * vi:tabstop=4 shiftwidth=4 expandtab
 */
